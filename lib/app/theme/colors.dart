import 'package:flutter/material.dart';

/// 프로젝트 에덴 컬러 팔레트
class EdenColors {
  EdenColors._();

  // ─── Primary (Sage Green) ───
  static const Color primary = Color(0xFF5B7553);
  static const Color primaryLight = Color(0xFF8BAF82);
  static const Color primaryDark = Color(0xFF3D5238);

  // ─── Secondary (Muted Sage) ───
  static const Color secondary = Color(0xFF8B9D83);
  static const Color secondaryLight = Color(0xFFB5C4AD);

  // ─── Accent (Warm Gold) ───
  static const Color accent = Color(0xFFC9A96E);
  static const Color accentLight = Color(0xFFD4B87C);
  static const Color accentDark = Color(0xFFA88B4A);

  // ─── Background ───
  static const Color backgroundLight = Color(0xFFFFFDF7);  // Warm Cream
  static const Color backgroundDark = Color(0xFF1C1C1E);

  // ─── Surface ───
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF2C2C2E);
  static const Color surfaceVariantLight = Color(0xFFF5F3ED);
  static const Color surfaceVariantDark = Color(0xFF3A3A3C);

  // ─── Text ───
  static const Color textPrimaryLight = Color(0xFF1C1C1E);
  static const Color textSecondaryLight = Color(0xFF6B6B6B);
  static const Color textTertiaryLight = Color(0xFF999999);
  static const Color textPrimaryDark = Color(0xFFF5F5F7);
  static const Color textSecondaryDark = Color(0xFFAAAAAA);
  static const Color textTertiaryDark = Color(0xFF777777);

  // ─── Verse Number ───
  static const Color verseNumberLight = Color(0xFF8B9D83);
  static const Color verseNumberDark = Color(0xFF6B8B63);

  // ─── Functional ───
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF5B7553);

  // ─── Divider ───
  static const Color dividerLight = Color(0xFFE8E5DE);
  static const Color dividerDark = Color(0xFF3A3A3C);
}
